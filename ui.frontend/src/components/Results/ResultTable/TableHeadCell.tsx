import { styled } from '@mui/system';
import { TableCell } from '@mui/material';

/**
 * Styled {@link TableCell} for the {@link ResultTableHead}.
 */
export const TableHeadCell = styled(TableCell)(() => ({
  fontWeight: 'bold',
}));
